image: golang:1.14

variables:
  GOFLAGS: -mod=vendor

build:
  stage: build
  script:
    - make
    - mkdir -p bin
    - cp /go/bin/terraform-provider-icinga2 bin/
  artifacts:
    name: terraform-provider-icinga2
    paths:
    - bin
